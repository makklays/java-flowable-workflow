spring:
  datasource:
    generate-unique-name: false
    url: jdbc:mariadb://localhost:3306/flowable_db
    username: admin
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      # password: myRedisPassword  # если установлен пароль, раскомментируй
      database: 0                 # номер базы (0–15, можно выбрать любую)
      timeout: 2000ms
  web:
    resources:
      media-types:
        wav: audio/wav
      static-locations: file:/home/alexander/IdeaProjects/java-flowable-workflow/src/main/resources/mystatic/
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
    database: default
    generate-ddl: false
    properties:
      connection:
        charSet: utf-8
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        ddl-auto: none
        show_sql: true
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baselineOnMigrate: true
    validateOnMigrate: true
    url: jdbc:mariadb://localhost:3306/flowable_db
    user: admin
    password: admin
    out-of-order: true
  thymeleaf:
    check-template-location: true
    prefix: classpath:/templates/  #this is the main
    suffix: .html
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
  messages:
    basename: messages
    encoding: UTF-8
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your_actual_key_here}
      chat:
        enabled: false
        options:
          model: gpt-3.5-turbo

management:
  endpoints:
    web:
      exposure:
        include: "*"
        exclude: []
  endpoint:
    prometheus:
      enabled: true

slack:
  webhook:
    url: ${SLACK_WEBHOOK_URL}

flowable:
  rest:
    app:
      enabled: false
  check-process-definitions: true
  cmmn:
    enabled: true
  dmn:
    enabled: true
  form:
    enabled: true
  content:
    enabled: true
  idm:
    enabled: true # Identity обычно нужен для пользователей

server:
  port: 8082

springdoc:
  info:
    title: Neural Network API
    version: 1.0
    description: API for Neural Network Management
  api-docs:
    enabled: true        # Включить /v3/api-docs
    path: /v3/api-docs   # Путь (по умолчанию /v3/api-docs)
  swagger-ui:
    enabled: true        # Включить Swagger UI
    path: /swagger-ui.html  # Путь, куда перенаправлять (но по факту UI — это /swagger-ui/index.html)
    try-it-out-enabled: true  # Кнопка "Try it out"

logging:
  level:
    org.flowable: INFO
    org.springframework.boot.devtools: WARN
    org.apache.kafka.clients: WARN
    org.apache.kafka.common: WARN
    org.springframework.kafka: INFO
    org.apache.tomcat: INFO
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type: WARN
    com.techmatrix18: DEBUG
    liquibase: WARN
    root: INFO
#    #org.thymeleaf: DEBUG