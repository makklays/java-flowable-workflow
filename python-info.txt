
// установка проекта
# mkdir python_app && cd python_app
// установка окружения среды python
# python3 -m venv venv
# source venv/bin/activate
(venv)# pip install requests
(venv)# pip freeze > requirements.txt
(venv)# which python3

// установка api (для создания REST API)
# pip install requests

//--------------------------------------
// создание файла
# nano hello.py

// заполнение файла
# В Python не нужно объявлять класс для запуска кода
def get_info(name):
    # f-строки — это очень удобный способ форматирования (аналог String.format)
    return f"Привет, {name}! Твой Python-интерпретатор работает из venv."

# Список (как ArrayList в Java)
tech_stack = ["Java", "Spring", "Flowable"]
tech_stack.append("Python") # Добавляем элемент

print(get_info("Alexander"))
print(f"Теперь твой стек: {', '.join(tech_stack)}")

# Простая проверка условия
if "Python" in tech_stack:
    print("Добро пожаловать в мир змей! 🐍")

// Запуск файла
python3 hello.py
//--------------------------------------

// REST API файл api_test.py
import requests

# Получим данные о цене биткоина или погоде через публичное API
response = requests.get("https://binance.com")
data = response.json()

print(f"Текущий курс BTC: {data['price']} USD")
//--------------------------------------

// Устанавливаем FastAPI
# pip install fastapi uvicorn

// Создаем ваш первый сервер server.py
from fastapi import FastAPI

# Создаем экземпляр приложения (аналог @SpringBootApplication)
app = FastAPI()

@app.get("/")
def read_root():
    return {"message": "Привет, Java-разработчик!", "target": "Flowable"}

@app.get("/calculate")
def calculate(a: int, b: int):
    # Пример логики, которую проще написать на Python
    result = a * b
    return {"operation": "multiplication", "result": result}

// Запускаем сервер!
# uvicorn server:app --reload
// Если всё ок, вы увидите: Uvicorn running on http://127.0.0.1:8000

// Проверяем curl "http://127.0.0"
// Должны получить: {"operation":"multiplication","result":50 }

// $ curl "http://127.0.0.1:8000"
// {"message":"Привет, Java-разработчик!","target":"Flowable"}

// Swagger: Перейдите в браузере на http://127.0.0. Вы увидите готовую интерактивную документацию (OpenAPI), которую FastAPI сделал сам!
//--------------------------------------

